<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

# Match user service logs
<match user-service.**>
  @type stdout
  <format>
    @type json
  </format>
</match>

# Match all other logs
<match **>
  @type stdout
</match>